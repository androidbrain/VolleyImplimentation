<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.vollyexam2909">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".order.OrderActivity"></activity>
        <activity android:name=".order.CartActivity" />
        <activity android:name=".property.ImageViewPagerActivity" />
        <activity android:name=".property.OpenImageActivity" />
        <activity android:name=".property.MoreImagesActivity" />
        <activity android:name=".property.PropertyActivity" />
        <activity android:name=".order.ProductsActivity" />
        <activity android:name=".pack.FourthActivity" />
        <activity android:name=".pack.ThirdActivity" />
        <activity android:name=".pack.FirstActivity" />
        <activity android:name=".pack.SecondActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>